# Production site URL (no trailing path) — used for SEO metadata, sitemap, and JSON-LD.
# Must match your Google Search Console property (same host; pick www or non-www and stick to it).
NEXT_PUBLIC_SITE_URL=https://natour.dev

# Existing app env (see your deployment provider)
